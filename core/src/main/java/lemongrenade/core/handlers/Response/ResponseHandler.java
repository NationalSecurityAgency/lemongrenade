package lemongrenade.core.handlers.Response;

import lemongrenade.core.handlers.Handler;

import javax.xml.ws.Response;

//Any empty handler for HTTP Responses
public class ResponseHandler implements Handler<Response> {

    //Perform some handling on a Response.
    @Override public void handle(Response input) {
        //TODO: Create an example of some handling we'd want to do on a Response object
    }
}
